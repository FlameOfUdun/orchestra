name: orchestra_generator_example
description: "Example project for orchestra_generator package"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  orchestra:
    path: ../../orchestra

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.11.1
  orchestra_generator:
    path: ../

flutter:
  uses-material-design: true
