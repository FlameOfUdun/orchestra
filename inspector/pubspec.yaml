name: orchestra_inspector
description: Flutter Inspector for the orchestra package. Provides visual debugging and insights into the orchestra architecture.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  orchestra:
    path: ../orchestra
  devtools_extensions: ^0.4.0
  devtools_app_shared: ^0.4.0
  vm_service: ^15.0.2
  graphview: ^1.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
