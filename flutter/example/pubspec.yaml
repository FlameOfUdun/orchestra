name: orchestra_example
description: "Example project for orchestra package"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: ^2.5.3
  orchestra: ^1.0.2
  orchestra_flutter:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  build_runner: 2.13.1
  orchestra_generator:
    path: ../../generator

flutter:
  uses-material-design: true
