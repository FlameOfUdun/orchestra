name: orchestra_flutter
description: Flutter widget integration for Orchestra — scope, reactive widgets, and handler API connecting the Orchestra runtime to the Flutter widget tree.
version: 1.0.1
issue_tracker: https://github.com/FlameOfUdun/orchestra/issues
repository: https://github.com/FlameOfUdun/orchestra
homepage: https://github.com/FlameOfUdun/orchestra
readme: README.md

topics:
  - state-management
  - architecture
  - reactive
  - widgets

environment:
  sdk: ^3.10.7
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  
  orchestra: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
